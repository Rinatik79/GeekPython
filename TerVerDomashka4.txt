Домашка 4:

1.  Среднее значение = 1 / (b - a);
    В нашема случае: 1 / (800 - 200) = 1/600

    МатОжидание MX = (200 + 800) / 2 = 500
    Дисперсия DX = (800 - 200)^2 / 12 = 30 000

2.

3.  f(x) = (1 / (4 * sqrt(2*pi))) * (exp(-(x+2)**2) / 32)
